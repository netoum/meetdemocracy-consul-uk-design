<%= form_tag(url, options) do |f| %>
  <%= hidden_current_filter_tag %>
  <%= render "govuk_publishing_components/components/search", {
    label_text: label,
    inline_label: true,
    aria_label: label,
    id: search_terms.to_s,
    name: :search,
    button_text: t("admin.shared.search.search")
  } %>
    <%= content %>
<% end %>

